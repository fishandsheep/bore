//! Tunnel lifecycle helpers for the web console.
//!
//! The current implementation keeps lifecycle logic in `state.rs` so the web
//! console can update shared state directly. This module remains as the home for
//! future extraction if the runtime grows more complex.
